import React from 'react';
import Header from './header'; // Import the Header component

const Register = () => {
    return (
        <div>
            <Header title="Register" subtitle="Page subtitle..." />
            {/* Page content goes here */}
        </div>
    );
};

export default Register;
