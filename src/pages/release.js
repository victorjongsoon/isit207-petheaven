import React from 'react';
import Header from './header'; // Import the Header component

const Release = () => {
    return (
        <div>
            <Header title="Release" subtitle="Page subtitle..." />
            {/* Page content goes here */}
        </div>
    );
};

export default Release;
