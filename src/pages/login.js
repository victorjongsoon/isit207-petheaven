import React from 'react';
import Header from './header'; // Import the Header component

const Login = () => {
    return (
        <div>
            <Header title="Login" subtitle="Page subtitle..." />
            {/* Page content goes here */}
        </div>
    );
};

export default Login;
